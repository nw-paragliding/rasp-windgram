;;; TO ALTER TOP OF LOOP SECTION
;;; (cannot contain procedures or functions)
;; tjo what can be done here?
;; remember that if it is done here but not queried with if() the program will probably override these.
;tjtest print("sequence ------------------  rasp.site_load.loop-top.ncl "+REGION+"-----------------------")
